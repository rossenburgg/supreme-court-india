export default function middleware() {
  // No internationalization logic; can be left empty or add custom logic if needed
}

export const config = {
  matcher: ['/((?!api|_next|_vercel|.*\\..*).*)']
}